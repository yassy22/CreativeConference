---
import fetchApi from '../lib/strapi';

const speakers = await fetchApi({
    endpoint: "speakers",
    wrappedByKey:"data",
    query:{
      populate:[ "image", "description","name"],
    }
  });




---




